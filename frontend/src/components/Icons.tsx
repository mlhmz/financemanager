import { Loader, LucideIcon, Plus, RotateCw, Trash } from "lucide-react";

export type Icon = LucideIcon;

export const Icons = {
  loader: Loader,
  plus: Plus,
  refresh: RotateCw,
  delete: Trash,
}